'use client';

import { Button } from '@/components/ui/button';

const Landing = () => {
  return (
    <section className="flex flex-col space-y-4 items-center h-[200vh] justify-center w-full">
      <p className="text-3xl font-bold leading-5"> Base template for Onelot Landing</p>
      <Button>Begin building</Button>
    </section>
  );
};

export default Landing;

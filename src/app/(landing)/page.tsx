import { Button } from "@/components/ui/button";
import React from "react";

const Landing = () => {
  return (
    <section className="flex flex-col space-y-4 items-center h-screen justify-center">
      <p className="text-3xl font-bold leading-5"> Base template for Onelot Landing</p>
      <Button>Begin building</Button>
    </section>
  );
};

export default Landing;
